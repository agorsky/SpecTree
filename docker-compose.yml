# Dispatcher Local Development Environment
# Run: docker-compose up -d

services:
  # ==========================================================================
  # API Backend
  # ==========================================================================
  api:
    build:
      context: .
      dockerfile: packages/api/Dockerfile
    container_name: dispatcher-api
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - PORT=3001
      - HOST=0.0.0.0
      - CORS_ORIGIN=http://localhost:5173
      - DATABASE_URL=file:/app/data/dispatcher.db
    volumes:
      - dispatcher-data:/app/data
    profiles:
      - full

  # ==========================================================================
  # Web Frontend
  # ==========================================================================
  web:
    build:
      context: .
      dockerfile: packages/web/Dockerfile
    container_name: dispatcher-web
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - api
    profiles:
      - full

volumes:
  dispatcher-data:
