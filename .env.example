# =============================================================================
# Local Development Environment Variables
# Copy this file to .env and update values as needed
# =============================================================================

# Database Configuration (local Docker SQL Server)
DB_HOST=localhost
DB_PORT=1433
DB_NAME=spectree
DB_USER=sa
DB_PASSWORD=LocalDev@Password123

# API Server Configuration
API_PORT=3001
API_HOST=0.0.0.0
CORS_ORIGIN=http://localhost:5173

# Application Settings
NODE_ENV=development

# Azure settings (only needed when deploying to Azure)
# AZURE_KEY_VAULT_URI=https://kv-spectree-dev.vault.azure.net/
# AZURE_CLIENT_ID=
