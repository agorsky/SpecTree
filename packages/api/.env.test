# =============================================================================
# Test Environment Variables
# Used by vitest for integration tests
# =============================================================================

# API Server Configuration
API_PORT=3002
API_HOST=0.0.0.0
CORS_ORIGIN=http://localhost:5173

# Application Settings
NODE_ENV=test

# JWT Configuration
JWT_SECRET=test-secret-key-for-jwt-signing-minimum-32-chars

# Database URL for Prisma (SQLite - SEPARATE TEST DATABASE)
# This prevents tests from affecting development/production data
DATABASE_URL="file:./data/spectree-test.db"
