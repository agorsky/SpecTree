{
  "name": "@spectree/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "SpecTree MCP server",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "spectree-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.3",
    "@spectree/api": "workspace:*",
    "@spectree/shared": "workspace:*",
    "zod": "^3.24.0"
  }
}
